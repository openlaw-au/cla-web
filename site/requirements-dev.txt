# Dev/test dependencies for site/ (the public CLQ landing-page builder).
# Install: python3 -m pip install -r site/requirements-dev.txt
# Run:     cd site && pytest --cov --cov-branch --cov-fail-under=100
pytest>=7.4
pytest-cov>=7.1
ruff>=0.6
black>=24.0
mypy>=1.10
