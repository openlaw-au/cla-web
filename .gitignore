
# generated static site
dist/
site/data/

# editor (Next.js app) build/tooling output
node_modules/
.next/
coverage/
playwright-report/
test-results/
.open-next/
.wrangler/
*.tsbuildinfo

# Next.js 16 auto-generates these agent-rules files under editor/ on dev/build
# (never at repo root, where CLAUDE.md is the hand-authored project doc — see
# force rule in root CLAUDE.md). Scoped to editor/ only so the root files aren't touched.
editor/AGENTS.md
editor/CLAUDE.md

# python (site/ tooling)
__pycache__/
*.pyc
.coverage
htmlcov/
.venv*/
